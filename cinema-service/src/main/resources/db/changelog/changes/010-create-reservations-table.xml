<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="010-create-reservations-table" author="Damian G">
        <createTable tableName="reservations">
            <column name="id" type="UUID" defaultValueComputed="gen_random_uuid()">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="basket_id" type="UUID">
                <constraints nullable="false"
                             foreignKeyName="fk_reservations_basket"
                             references="baskets(id)"/>
            </column>

            <column name="screening_id" type="UUID">
                <constraints nullable="false"
                             foreignKeyName="fk_reservations_screening"
                             references="screenings(id)"/>
            </column>

            <column name="seat_id" type="UUID">
                <constraints nullable="false"
                             foreignKeyName="fk_reservations_seat"
                             references="seats(id)"/>
            </column>

            <column name="status" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>

            <column name="pricing_type" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>

            <column name="order_id" type="UUID">
                <constraints nullable="true"
                             foreignKeyName="fk_reservations_order"
                             references="orders(id)"/>
            </column>

            <column name="created_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="now()">
                <constraints nullable="false"/>
            </column>

            <column name="version" type="INT" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <sql>
            CREATE UNIQUE INDEX uq_reservation_per_seat_screening
                ON reservations(screening_id, seat_id)
                WHERE status IN ('CREATED', 'PAID');
        </sql>
    </changeSet>

</databaseChangeLog>