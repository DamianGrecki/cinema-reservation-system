<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="006-create-activation-tokens-table" author="Damian G">

        <sql>
            CREATE EXTENSION IF NOT EXISTS pgcrypto;
        </sql>

        <createTable tableName="activation_tokens">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="token" type="UUID">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="expiration_date" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="is_used" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>

            <column name="user_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="activation_tokens"
                baseColumnNames="user_id"
                referencedTableName="users"
                referencedColumnNames="id"
                onDelete="CASCADE"
                constraintName="fk_activation_tokens_user"/>

        <createIndex indexName="idx_activation_token_token" tableName="activation_tokens">
            <column name="token"/>
        </createIndex>

        <createIndex indexName="idx_activation_tokens_user_is_used" tableName="activation_tokens">
            <column name="user_id"/>
            <column name="is_used"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>
