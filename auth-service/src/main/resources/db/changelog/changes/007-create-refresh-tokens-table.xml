<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="007-create-refresh-tokens-table" author="Damian G">

        <createTable tableName="refresh_tokens">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="token" type="UUID">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="expiration_date" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>

            <column name="revoked" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>

            <column name="user_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="refresh_tokens"
                baseColumnNames="user_id"
                referencedTableName="users"
                referencedColumnNames="id"
                onDelete="CASCADE"
                constraintName="fk_refresh_tokens_user"/>

        <createIndex indexName="idx_refresh_token_token" tableName="refresh_tokens">
            <column name="token"/>
        </createIndex>

        <createIndex indexName="idx_refresh_tokens_user_revoked" tableName="refresh_tokens">
            <column name="user_id"/>
            <column name="revoked"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>
